export const environment = {
  production: true,
  apiUrl: 'https://api-stage.bidsquad.net/v1/',
  role: 'admin', // This should not be changed
  totalRecordsToShow: 10
};

// To run This Environment please run this command "ng build --configuration=staging"
